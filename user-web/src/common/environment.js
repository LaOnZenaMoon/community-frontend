const ENV_LOCAL = 'local';
const ENV_DEV = 'dev';
const ENV_PROD = 'prod';

const CURRENT_ENV = ENV_LOCAL;
const GATEWAY_SERVER = 'http://localhost:8890';

export {
  ENV_LOCAL,
  ENV_DEV,
  ENV_PROD,
  CURRENT_ENV,
  GATEWAY_SERVER,
};
